# WARNING: This file is generated by the bump.sh script. Do not edit it manually.

# env describes things which I expect will change frequently
# (to the point of needing automation to keep up to date).
env:
  rust_pins:
    beta: &rust_beta "1.86.0-beta.7"
  llvm_for_rust_pins:
    stable: &llvm_stable "19"
  nixpkgs:
    unstable: &nixpkgs_unstable "0740f6f238767d4caf9afe774d3e88105766dfc6"
  just:
    version: &just_version "1.40.0"
# matrix describes the test structure which I expect to be relatively stable.
matrix:
  toolchain:
    - &default
      key: "beta"
      llvm: *llvm_stable
      rust:
        channel: "beta"
        version: *rust_beta
      sysroot:
        profile:
          - "release"
          - "debug"
        target:
          - "x86_64-unknown-linux-gnu"
      just: *just_version
  nixpkgs:
    - *nixpkgs_unstable
